name: kline_chart
description: Flutter native candlestick chart without dependencies, high performance, smooth scrolling, supports multiple indicators
version: 1.0.2

homepage: https://github.com/ascenx/kline_chart
repository: https://github.com/ascenx/kline_chart


environment:
  sdk: '>=2.17.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
